## 1. 主题模式基础

- [x] 1.1 添加应用主题模式模型，包含 `system`、`light`、`dark` 三个值。
- [x] 1.2 添加所选主题模式的持久化存储，默认值为 `system`。
- [x] 1.3 在 Activity 渲染主要内容前，通过 AppCompat 夜间模式应用已保存的主题模式。
- [x] 1.4 提供主题选择 UI 入口，让用户可以选择跟随系统、浅色或暗色。

## 2. 主题资源

- [x] 2.1 将 `Theme.TestAi` 改为兼容 day/night 的 AppCompat 父主题。
- [x] 2.2 为应用背景、表面、主要文字、次要文字、分割线和控件添加语义化颜色资源。
- [x] 2.3 为语义化颜色和主题属性添加 `values-night` 覆盖。
- [x] 2.4 更新依赖主题的 drawable，使其使用语义化颜色或夜间专用替代资源。

## 3. 页面迁移

- [x] 3.1 将 XML 布局中的浅色专用背景和文字颜色替换为主题感知资源或属性。
- [x] 3.2 更新弹窗和底部面板的布局/drawable，确保其在暗色模式下正确渲染。
- [x] 3.3 更新 Compose 主题使用方式，确保用户显式覆盖和系统模式与 AppCompat 得到相同的最终生效模式。
- [x] 3.4 更新影响可读性的自定义 View、adapter 和代码颜色赋值，使其解析主题感知颜色。

## 4. 验证

- [x] 4.1 为持久化主题模式映射添加聚焦测试或验证覆盖。
- [x] 4.2 使用 `./gradlew :app:assembleDebug` 成功构建 debug 应用。
- [ ] 4.3 在浅色、暗色、跟随系统三种模式下人工验证代表性页面。
- [x] 4.4 检查主页面、网格页面、订单确认页、弹窗、底部面板和自定义 View 示例，确认对比度清晰可读。
